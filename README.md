# Mole Racing Track Builder – Prototype 12

Open `index.html` directly in your browser.

## Changed in Prototype 12

- The rotation handle now uses a custom clockwise rotate cursor.
- The cursor hotspot is centered on the icon.
- A normal grab cursor is used as fallback if the custom SVG cursor is unsupported.
- While actively dragging the rotation handle, the cursor changes to grabbing.
